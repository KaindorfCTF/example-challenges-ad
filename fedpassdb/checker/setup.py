#!/usr/bin/env python

from distutils.core import setup

setup(name='Distutils',
      version='1.0',
      description='Checker for Challenge FedPassDb',
      author='exo',
      packages=['fedpassdb'],
     )
